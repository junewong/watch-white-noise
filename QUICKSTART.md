# 快速开始指南

## 方式一：GitHub Actions 自动构建（推荐）

### 1. 推送代码到 GitHub

```bash
cd /Users/junewong/codes/android/waitch-white-noise

# 初始化 Git 仓库（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: 白噪音手表应用"

# 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/waitch-white-noise.git

# 推送到 GitHub
git push -u origin main
```

### 2. 等待自动构建

- 访问你的 GitHub 仓库
- 点击 "Actions" 标签
- 等待构建完成（约 3-5 分钟）
- 构建成功后，点击构建任务
- 下载 "app-debug" artifact
- 解压得到 `app-debug.apk`

### 3. 安装到手表

```bash
# 连接手表到电脑
adb devices

# 安装 APK
adb install app-debug.apk
```

## 方式二：本地构建

### 1. 初始化 Gradle Wrapper

```bash
cd /Users/junewong/codes/android/waitch-white-noise
./init.sh
```

### 2. 构建 APK

```bash
./gradlew assembleDebug
```

### 3. 安装

```bash
adb install app/build/outputs/apk/debug/app-debug.apk
```

## 使用说明

1. **打开应用** - 自动开始播放雨声
2. **暂停/播放** - 点击中央大按钮
3. **设置定时** - 点击"定时"区域，选择时长
4. **查看剩余时间** - 底部显示倒计时

## 省电提示

- ✅ 播放时可以关闭屏幕，音乐继续播放
- ✅ 暂停后切换到后台，5秒后自动退出应用
- ✅ 定时到期后自动淡出并退出

## 故障排除

### 问题：GitHub Actions 构建失败

**解决方案**：
- 检查 `.github/workflows/build.yml` 文件是否存在
- 确保仓库的 Actions 功能已启用
- 查看构建日志获取详细错误信息

### 问题：本地构建失败 "gradle-wrapper.jar not found"

**解决方案**：
```bash
./init.sh
```

### 问题：无法安装到手表

**解决方案**：
```bash
# 检查设备连接
adb devices

# 如果没有设备，启用手表的开发者模式和 USB 调试
# OPPO Watch 3: 设置 -> 关于手表 -> 连续点击版本号 7 次
```

### 问题：应用闪退

**解决方案**：
- 确保手表系统版本 >= Android 11
- 检查是否授予了必要的权限
- 查看 logcat 日志：`adb logcat | grep witenoise`

## 项目结构

```
waitch-white-noise/
├── app/                        # 应用模块
│   ├── src/main/
│   │   ├── java/              # Java 源代码
│   │   ├── res/               # 资源文件
│   │   └── AndroidManifest.xml
│   └── build.gradle
├── docs/                       # 文档
│   ├── design.md              # 设计文档
│   ├── oppo-watch3.md         # 手表规格
│   └── project-summary.md     # 项目总结
├── music/                      # 原始音频文件
│   └── 0.m4a
├── .github/workflows/          # CI/CD
│   └── build.yml
├── build.gradle               # 项目构建配置
├── settings.gradle            # 项目设置
├── gradlew                    # Gradle Wrapper
├── init.sh                    # 初始化脚本
├── README.md                  # 项目说明
└── QUICKSTART.md              # 本文件
```

## 下一步

- 🎵 添加更多白噪音音频文件到 `music/` 目录
- 🎨 自定义 UI 颜色和主题
- ⏰ 调整定时选项
- 📱 测试在不同手表上的兼容性

## 需要帮助？

查看详细文档：
- [设计文档](docs/design.md) - 技术架构和实现细节
- [项目总结](docs/project-summary.md) - 功能清单和使用说明
- [README](README.md) - 项目概述

---

**祝你使用愉快！** 🎧✨
